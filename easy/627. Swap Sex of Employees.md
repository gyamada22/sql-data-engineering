# 627. Swap Sex of Employees

**Data:** 27/12/2025  
**Dificuldade:** Easy  
**Tópicos:** SQL, UPDATE, CASE  
**Link:** https://leetcode.com/problems/swap-sex-of-employees/description/?source=submission-ac

## Descrição
A tabela `Salary` possui uma coluna `sex` com os valores `'m'` e `'f'`.

O objetivo é **trocar os valores da coluna `sex` diretamente na tabela**, seguindo as regras:
- `'m'` → `'f'`
- `'f'` → `'m'`

⚠️ A atualização deve ser feita **in place**, sem utilizar `SELECT`.

---

## 🧠 Abordagem

Utilizar um **único comando `UPDATE`** com uma expressão `CASE`, permitindo:
- Avaliar o valor atual de `sex`
- Atribuir o valor oposto na mesma operação
- Evitar múltiplos updates ou soluções intermediárias

Essa abordagem é segura, simples e atende exatamente às restrições do problema.

---

## 💻 Solução (SQL Server)

```sql
UPDATE Salary
SET sex = CASE
              WHEN sex = 'f' THEN 'm'
              WHEN sex = 'm' THEN 'f'
          END;
